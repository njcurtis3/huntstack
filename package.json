{
  "name": "huntsource",
  "version": "0.1.0",
  "private": true,
  "description": "Comprehensive hunting information platform",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:web": "pnpm --filter @huntsource/web dev",
    "dev:api": "pnpm --filter @huntsource/api dev",
    "build": "pnpm run --parallel build",
    "build:web": "pnpm --filter @huntsource/web build",
    "build:api": "pnpm --filter @huntsource/api build",
    "lint": "pnpm run --parallel lint",
    "test": "pnpm run --parallel test",
    "db:generate": "pnpm --filter @huntsource/db generate",
    "db:migrate": "pnpm --filter @huntsource/db migrate",
    "db:push": "pnpm --filter @huntsource/db push",
    "db:studio": "pnpm --filter @huntsource/db studio",
    "scrape:node": "pnpm --filter @huntsource/scrapers-node start",
    "scrape:python": "cd apps/scrapers-python && python -m scrapy crawl",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules"
  },
  "keywords": [
    "hunting",
    "wildlife",
    "regulations",
    "migratory-birds",
    "big-game"
  ],
  "author": "",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
